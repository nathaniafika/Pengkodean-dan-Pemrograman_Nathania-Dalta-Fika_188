-- Buat database
CREATE DATABASE IF NOT EXISTS perpustakaan;
USE perpustakaan;

-- Tabel: Anggota
CREATE TABLE anggota (
  id_anggota INT PRIMARY KEY AUTO_INCREMENT,
  nama VARCHAR(100),
  alamat TEXT,
  no_telepon VARCHAR(15)
);

-- Tabel: Buku
CREATE TABLE buku (
  id_buku INT PRIMARY KEY AUTO_INCREMENT,
  judul VARCHAR(150),
  pengarang VARCHAR(100),
  penerbit VARCHAR(100),
  tahun_terbit YEAR,
  stok INT
);

-- Tabel: Petugas
CREATE TABLE petugas (
  id_petugas INT PRIMARY KEY AUTO_INCREMENT,
  nama_petugas VARCHAR(100),
  username VARCHAR(50),
  password VARCHAR(255)
);

-- Tabel: Peminjaman
CREATE TABLE peminjaman (
  id_peminjaman INT PRIMARY KEY AUTO_INCREMENT,
  id_anggota INT,
  id_petugas INT,
  tanggal_pinjam DATE,
  tanggal_kembali DATE,
  FOREIGN KEY (id_anggota) REFERENCES anggota(id_anggota),
  FOREIGN KEY (id_petugas) REFERENCES petugas(id_petugas)
);

-- Tabel: Detail_Pinjam
CREATE TABLE detail_pinjam (
  id_detail INT PRIMARY KEY AUTO_INCREMENT,
  id_peminjaman INT,
  id_buku INT,
  jumlah INT,
  FOREIGN KEY (id_peminjaman) REFERENCES peminjaman(id_peminjaman),
  FOREIGN KEY (id_buku) REFERENCES buku(id_buku)
);

-- Data untuk tabel: Anggota
INSERT INTO anggota (nama, alamat, no_telepon) VALUES
('Ayu Lestari', 'Jl. Melati No. 10', '081234567890'),
('Budi Santoso', 'Jl. Mawar No. 15', '082345678901'),
('Citra Dewi', 'Jl. Kenanga No. 20', '083456789012');

-- Data untuk tabel: Buku
INSERT INTO buku (judul, pengarang, penerbit, tahun_terbit, stok) VALUES
('Dasar-Dasar Akuntansi', 'Sugiyono', 'Andi', 2020, 5),
('Pemrograman Web', 'Yuliana', 'Erlangga', 2019, 3),
('Statistika Ekonomi', 'Bambang Supriyadi', 'Salemba Empat', 2021, 4);

-- Data untuk tabel: Petugas
INSERT INTO petugas (nama_petugas, username, password) VALUES
('Dina Rahma', 'dina123', 'pass123'),
('Eko Saputra', 'eko2024', 'admin2024');

-- Data untuk tabel: Peminjaman
INSERT INTO peminjaman (id_anggota, id_petugas, tanggal_pinjam, tanggal_kembali) VALUES
(1, 1, '2025-04-10', '2025-04-17'),
(2, 2, '2025-04-12', '2025-04-19');

-- Data untuk tabel: Detail_Pinjam
INSERT INTO detail_pinjam (id_peminjaman, id_buku, jumlah) VALUES
(1, 1, 1),
(1, 2, 1),
(2, 3, 2);
